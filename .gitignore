# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (KEEP SECRETS OUT OF GIT)
.env
.env*.local
.env.local
serviceAccountKey.json

# typescript
*.tsbuildinfo

# uploaded files (folder file store) — keep the folder, ignore the contents
/public/uploads/*
!/public/uploads/.gitkeep

# local-only helper scripts & tooling (not for the repo)
/enable-auth.cjs
/verify-firestore.cjs
/.claude/
